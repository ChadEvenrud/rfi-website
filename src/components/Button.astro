---
import type { ButtonVariant } from '../data/brand';
import { href as withBase } from '../utils/url';

interface Props {
	variant?: ButtonVariant;
	size?: 'sm' | 'md' | 'lg';
	href?: string;
	type?: 'button' | 'submit' | 'reset';
	class?: string;
}

const {
	variant = 'primary',
	size = 'md',
	href,
	type = 'button',
	class: className = '',
} = Astro.props;

const linkHref = href ? withBase(href) : undefined;

const variantClasses: Record<ButtonVariant, string> = {
	primary: 'btn-primary',
	secondary: 'btn-secondary',
	ghost: 'btn-ghost',
};

const sizeClasses = {
	sm: 'btn-sm',
	md: '',
	lg: 'btn-lg',
};

const classes = [variantClasses[variant], sizeClasses[size], className].filter(Boolean).join(' ');
---

{
	linkHref ? (
		<a href={linkHref} class={classes}>
			<slot />
		</a>
	) : (
		<button type={type} class={classes}>
			<slot />
		</button>
	)
}
